# Steam 封禁批量查询器 · 自动登录版

本文适用于 `v3.1.3`。除批量查 VAC/游戏封禁、PUBG 判断、导入导出、API Key 加密保存与分页列表外，v2 修复了 CSV 导入/导出、未解析 SteamID64 的查询，以及账号登录配置写入的边界情况；v3 进一步修复了账号合并、无表头密码、登录重入和筛选误操作问题，并加入实时查询进度、登录计时和 GitHub 更新提醒。

---

## 一、改了什么

### 0.4. v3.1.3：跨版本自动继承账号库和 Key
- **固定数据目录**：账号库与保存的 Key 不再跟随某一个 EXE 文件夹，而是统一存到当前 Windows 用户的 `%LOCALAPPDATA%\PUBG-Auto-Login\`。以后即使把新版 EXE 下载到不同目录，仍会自动使用同一份账号、查询记录、登录计时和已保存的 Key。
- **首次升级自动迁移**：若共享数据目录还没有账号库，程序只会检查当前 EXE 目录、下载、桌面、文档及其一级子目录中的固定文件名 `steam_ban_accounts.sqlite3`，选取最近更新的一份迁移；不会递归扫描其它个人文件。
- **找不到旧版时可手动迁移**：点击「迁移旧版数据」→ 选择旧版本旁边的 `steam_ban_accounts.sqlite3`。若当前共享库已有数据，程序会先询问是否替换，并将当前库备份为 `steam_ban_accounts.before-migration-时间.sqlite3`。同目录的 `steam_ban_settings.json` 也会一并迁移；DPAPI 仍绑定当前 Windows 用户，因此账号密码和 API Key 的加密可继续使用。

### 0.3. v3.1.2：在程序内申请 Steam Web API Key
- **Key 输入区新增官方申请入口**：`Steam Web API Key` 标题右侧显示并可点击打开 `https://steamcommunity.com/dev/apikey`，不需要用户另行搜索。
- **简短申请说明**：登录 Steam 后填写网站域名（个人使用可填 `localhost`）→ 注册 → 复制生成的 Key → 粘贴到程序输入框并点击「保存 Key」。Key 只会用当前 Windows 用户的 DPAPI 加密保存在本机，不能分享给他人。

### 0.2. v3.1.1：更新检查容错与长列表位置保持
- **更新检查不会卡住按钮**：GitHub、公司代理或网络劫持页返回的内容会先校验为 JSON 对象；数组、字符串、HTML、字段异常和后台线程启动异常都会作为一次失败处理，右上角按钮会恢复为「检查更新」，可再次尝试，不会永久显示「正在检查」。
- **整点更新不会跳回列表顶部**：登录计时跨整点时，仅原地更新当前已加载账号的「上次登录」单元格；不会清空 Treeview、重新加载第一页，也不会丢失大列表的滚动位置、已加载进度、勾选或行选中。

### 0.1. v3.1.0：登录计时、最近账号排序与 GitHub 更新提醒
- **登录计时**：本工具成功自动登录过的账号会记录在本机数据库中。当下一次使用本工具切换账号、并成功退出该账号的 Steam 客户端时，计时从该退出时刻开始；72 小时内列表显示“距上次登录 N 小时”，超过 72 小时显示“上次登录：YYYY-MM-DD”。尚未从本工具切出的最近账号显示“当前登录中”。手动关闭 Steam 的动作无法被桌面工具可靠获知，因此不会提前写入退出时间。
- **最近登录优先**：至少成功自动登录过一次的账号会排在从未登录账号前面；多个登录过的账号按最近成功登录时间从新到旧排序。
- **GitHub 云更新提醒**：启动后后台请求 GitHub 的 Latest Release。若标签版本高于本地版本，右上角“检查更新”会变为“下载 vX.Y.Z”；点击后打开对应 Release 页面。该功能不上传账号、密码或 API Key，且不会静默下载、替换或执行 EXE。

### 0. v3.0.0：导入、登录、筛选与进度修复
- **登录防重复启动**：自动登录开始后会进入「登录进行中」状态；按钮重复点击、列表双击或右键再次登录都会被拦截，直到后台登录完成或失败，避免两个线程互相结束 Steam、争抢登录窗口。
- **无表头 CSV 不再丢密码**：`账号,密码,备注` 会正确保存密码并以账号名暂代 SteamID64；`账号,SteamID64,备注` 与 `SteamID64,备注` 仍按原格式识别。
- **同账号只保留一行**：从 TXT/JSON 先导入「账号+密码」后，再从 CSV 补入该账号的真实 SteamID64，会更新同一行并保留已有密码，不会拆成两条记录。
- **筛选切换更安全**：进入「仅显示有风险/异常」或「仅显示无风险」时，程序会清空勾选与行选中；关闭筛选只刷新列表，不会再清空一次。删除和导出不会作用于看不见的旧勾选行。
- **TXT 可安全往返**：导出的 `账号----密码----SteamID64` 在密码本身含 `----` 时，重新导入会从行尾识别 SteamID64 并还原完整密码。
- **真实进度**：JSON 导入按实际已解析记录数递增；封禁查询界面新增进度条，实时显示「已完成 / 总数 / 百分比」，包括解析缺失 SteamID64 的等待阶段。

### 1. 导入 CSV 时会保存「账号 + 密码 + SteamID64」
- 数据库新增 `password_enc` 字段；密码用 **Windows DPAPI（当前 Windows 用户）** 加密后存入本机 `steam_ban_accounts.sqlite3`，不是明文。
- CSV 列识别：`account` → 账号名、`steam_id` → SteamID64、`password` → Steam 密码（自动避开 `email_password`，不会把邮箱密码当 Steam 密码存）。
- `v2.0.0` 支持只有两列的 `account,password` CSV：没有 SteamID64 时，会先以账号名占位；点「开始查询」后自动补全真实 SteamID64。
- `v2.0.0` 修复了**无表头 CSV** 的首行丢失问题；第一行会和后续行一样正常导入。
- `v2.0.0` 修复了 CSV 导出字段错位问题；导出的 `account,password,steam_id,note` 文件可以直接重新导入。
- **旧数据库自动升级**：程序启动时检测到缺列会自动 `ALTER TABLE` 补上，原来的查询结果和账号不会丢，不需要重新建库。

实测（导入一份 2900 行的账号清单）：
| 检查项 | 结果 |
|---|---|
| 导入账号数 | 2900 / 2900 |
| 保存密码数 | 2900 |
| 逐条解密回原文比对 | 全部一致，0 条不符 |
| 在 .sqlite3 文件里搜明文密码 | 搜不到（确认加密生效） |

### 2. 「登录 Steam」按钮 → 「自动登录所选账号」
点一下就完成：**结束当前 Steam → 用选中账号的账号密码启动 Steam**，不需要手输账号密码。已经解析出 SteamID64 的账号，还会同步改写自动登录配置。

已有 SteamID64 的账号会按以下步骤额外写入自动登录配置（与开源项目 TcNo Account Switcher 的官方文档算法一致）；未解析 SteamID64 的账号会跳过第 2、3 步，直接执行客户端登录和密码填充：
1. 结束正在运行的 Steam（先发 `steam.exe -shutdown` 正常退出，超时再强制结束）；
2. 改写 `Steam\config\loginusers.vdf`：把目标账号设为 `RememberPassword=1`、`AutoLogin=1`、`MostRecent=1`（改写前自动备份为 `loginusers.vdf.bak`，解析失败则放弃写入以免清空账号列表）；
3. 同步注册表 `HKCU\Software\Valve\Steam\AutoLoginUser`；
4. 用 `steam.exe -noreactlogin -login <账号> <密码>` 启动客户端；
5. **自动填密码**：实测现在的 Steam 已经不再接受命令行上的密码，只会弹出「登录 Steam」窗口（账号名已填好）。
   所以程序改用**输入注入**的办法：等登录窗口出现 → 切到前台 → 用剪贴板粘贴密码（Ctrl+V）→ 回车。
   若第一次没生效，会自动再试「先按 Tab」「用鼠标点密码框」两种方式；填完立刻清空剪贴板。
   只有确认前台窗口确实是 Steam 登录窗口时才会输入，避免密码被打进别的程序。

没保存密码时：直接弹出密码输入框，输入后即加密保存并继续登录，不用先去编辑账号。
**登录过程不再有任何确认弹窗**：只要当前有别的 Steam 账号在线，就直接退出并切换到所选账号。

> `v2.0.0`：如果账号还没有 SteamID64（例如刚从 `account,password` CSV 或 `账号----密码` TXT 导入），仍可直接自动登录；程序会**跳过** `loginusers.vdf`、注册表和按账号目录写 Steam 设置这些依赖 SteamID64 的步骤，避免写入无效配置。待「开始查询」自动解析并保存 SteamID64 后，下一次登录会照常写入这些配置。

---

## 二、怎么用

1. **从 v3.1.3 起，新 EXE 不必放到旧 EXE 同一目录**——程序会统一使用 `%LOCALAPPDATA%\PUBG-Auto-Login\` 的数据。首次升级会自动迁移常见目录中的旧版数据；未自动找到时点击「迁移旧版数据」，选择旧版旁边的 `steam_ban_accounts.sqlite3`。
2. 导入账号清单：完整 CSV 可用 `account,password,steam_id,note`；只有账号密码时可直接使用 `account,password` 两列表头 CSV、无表头 `账号,密码,备注`、`账号----密码` TXT，或「粘贴导入」。无表头 CSV 的第一行也会被导入。
3. 在列表里**选中一个账号** → 点 **「自动登录所选账号」**。
4. 需要查询封禁或写入完整 Steam 客户端配置时，点击「开始查询」先解析尚未有 SteamID64 的账号。
5. 如果当前有别的 Steam 账号在线，程序会**直接退出**它并切换过去，不再弹窗询问。

---

## 三、必须知道的现实限制（重要）

1. **每个账号在这台电脑上第一次登录，Valve 会要求 Steam Guard 验证码**（邮箱验证码或手机令牌）。
   这一步**无法绕过**——这是 Valve 的反盗号机制，任何声称能绕过的脚本/工具都是假的。
   - 你在客户端里完成这一次验证后，该账号在本机就成为「可信设备」（Steam 会生成 ssfn 信任文件），
     **以后切换该账号不再需要密码，也不再需要验证码**。
   - 也就是说：第一次要点一下验证码，之后就是纯自动。
2. 如果账号带 **shared_secret（maFile 里的手机令牌密钥）**，可以做到连验证码都自动生成填入。
   你的 CSV 里没有这一列；如果你手上有 maFile，告诉我，我再加这个能力。
3. CSV 里的 `email / email_password / email_url` **目前没有使用**：
   自动读邮箱验证码这条路，Outlook/Hotmail 个人邮箱自 2024-09-16 起已停用「账号密码式 IMAP」，
   需要走 OAuth，可靠性差；所以没有做。

## 四、风险提醒（请自己判断）

- **限流**：同一台机器/同一个 IP 短时间内登录大量不同账号，Steam 可能触发登录限流或临时锁定。建议放慢节奏、不要连续高频切换。
- **协议风险**：Steam 用户协议禁止账号买卖与共享，批量登录这类账号存在被封禁的风险，请自行评估。
- **本机安全**：密码以 DPAPI 加密存储，**只有当前 Windows 用户能解密**。换 Windows 用户或把数据库拷到别的机器，密码解不开（表现为「没有保存密码」，需要重新输入）。别把这个数据库文件发给别人。

---

---

## 五、登录时自动写入的 Steam 客户端设置（新增）

对于已解析 SteamID64 的账号，点「自动登录所选账号」时会先写入三项 Steam 客户端设置（都是 Steam 自己的配置项），**原文件先备份**到
`<Steam>\userdata\<账号ID>\config\steam_ban_tool_backup\`（localconfig.vdf / sharedconfig.vdf）。

| 选项 | 写入位置 | 写入内容 |
|---|---|---|
| 登录首页改为库 | 三处都写：`userdata\<id>\7\remote\sharedconfig.vdf`、`userdata\<id>\config\localconfig.vdf`、全局 `config\config.vdf` | `Software\Valve\Steam\SteamDefaultDialog = "#app_games"` |
| 关闭推销广告弹窗 | `userdata\<id>\config\localconfig.vdf` | `news\NotifyAvailableGames = "0"` |
| 好友窗口不自动弹出（**不影响在线状态**） | `userdata\<id>\config\localconfig.vdf` 的 `WebStorage\ChatStorePopupState_<id>`；`userdata\<id>\7\remote\sharedconfig.vdf` 的 `FriendsUI\FriendsUIJSON` | 窗口状态 `bFriendsListVisible=false`、`always_restore=false`；同时把 `bSignIntoFriends` / `friends\SignIntoFriends` 置为 **true/1**（保证账号在线） |

> ⚠️ 修正记录（2026-09-22）：早期版本把 `SignIntoFriends` 设成了 0，
> 而那个开关同时决定「要不要登录好友系统」—— 于是账号一进来就是**离线**状态。
> 现已改为只隐藏好友窗口（`ChatStorePopupState`），并把两个 sign-in 开关恢复成开。
> 已经被写成离线的账号，**再点一次「自动登录所选账号」就会自动改回来**；
> 当前已经登录的会话也可以直接在好友菜单里点「在线」立刻恢复。

**这些键名怎么来的**：都对应 Steam 客户端自己的设置 —
SteamUI.dll 里就有 `news\NotifyAvailableGames`、`enable_marketing_messages`、`SteamDefaultDialog` 这些定义，
账号的 `sharedconfig.vdf` 里本来也会出现 `"SteamDefaultDialog"  "#app_games"`（在 Steam 设置里把首屏改成"库"就会写进去）。
程序只是替你在登录前把这些值写好。

另外登录命令行带了 `-noreactlogin -login`：强制走旧版登录框，账号名会被预填好，密码由上面的输入注入补上。

**几点注意**：

- **首次登录的新账号**：在 SteamID64 已解析但本机还没有 `userdata\<账号ID>` 时，程序会**自动建好目录和最小配置文件再写入**（这是上一版的漏洞：文件不存在时它什么也没写、还不提示，所以第一次登录的账号完全没效果）。
- 好友设置除了本地配置，还会写进 `sharedconfig.vdf` 的 `FriendsUI\FriendsUIJSON`；程序会把 `bSignIntoFriends` 保持为 `true`，避免账号离线。
- **关闭好友列表**只是不自动弹出好友窗口，不会关闭好友系统，也不会让账号显示为离线。
- **云端覆盖**：如果这个账号在别处登录过，Steam 可能在登录时用云端配置盖掉本地修改；重新点一次「自动登录所选账号」即可再次写入。

---

## 六、导入格式与自动解析 SteamID64（新增）

### 三种导入入口

| 入口 | 说明 |
|---|---|
| **粘贴导入（账号----密码）** | 直接弹一个文本框，从记事本 / 网页 / 表格里复制粘贴即可；**不需要是文件、不需要是 txt** |
| **导入文件** | 选择文件，支持 CSV / TSV / JSON / TXT；**后缀不认识也没关系**（.dat / .log / 无后缀都行）——程序会看内容判断：第一行带 `----` 就当「账号----密码」文本，以 `{` 或 `[` 开头就当 JSON |
| 拖不进来的情况 | 只有账号密码时用「粘贴导入」最快 |

### 支持三种导入格式

| 格式 | 说明 |
|---|---|
| CSV / TSV | 按表头识别（`account` / `steam_id` / `password` / `note` …）；支持完整的 `account,password,steam_id,note`，也支持只有 `account,password` 的两列表头。无表头 CSV 的第一行不会丢失；常用列顺序为 `账号,SteamID64,备注`、`SteamID64,备注`，或 `账号,密码,备注`。 |
| JSON | 支持 `[{...}]`、`{"accounts":[{...}]}`、`{"账号":"密码"}`、`{"账号":{"password":...}}`；字段名兼容 `account/username/账号`、`password/密码`、`steam_id/steamid64`、`note/备注` |
| TXT | 一行一个：`账号----密码`；也支持 `账号----密码----备注` 或 `账号----密码----7656119…`。**第三段若恰为 17 位数字，一律解释为 SteamID64；若想把纯 17 位数字作为备注，请改用 CSV/JSON 或给备注加非数字字符。** 导出格式会从行尾识别 SteamID64，因此密码本身含分隔符也能往返；分隔符自动识别（`----` / `---` / `--` / Tab / `|` / `：` / `:` / `;` / `,`） |

### 只有账号密码、没有 64 位 ID 也能查

导入时如果没有 SteamID64，程序会用**账号名占位**，列表里显示「未解析（查询时自动获取）」。
点「开始查询」后会自动补全：

1. 用「账号 + 密码」调用 Valve 的 `IAuthenticationService`（与开源项目 steampy 同一套协议）：
   `GetPasswordRSAPublicKey` → 用公钥做 RSA(PKCS#1 v1.5) 加密密码 → `BeginAuthSessionViaCredentials`；
2. 从返回里取出 `steamid`，**不需要等 Steam Guard 验证码**（拿到就放弃这次会话）；
3. 写回数据库，再照常用 Steam Web API 查询封禁状态。

**注意（重要）**

- 每个缺 ID 的账号会发一次认证请求，程序**每个账号间隔 1 秒**，并且**连续失败 5 次就自动停止**并提示，避免触发 Steam 限流；
- 密码错误 → 该行标记「查询失败：无法解析 SteamID64：账号或密码不正确」；
- 没有保存密码的账号会跳过，提示「缺少密码，无法解析 SteamID64」；
- 如果账号开了手机令牌，Steam 可能会给手机推一条登录请求通知 —— 程序拿到 ID 就结束，不会真的登录客户端。

实测：只用「账号----密码」的 TXT 导入 3 个账号，解析出的 SteamID64 与 CSV 里的完全一致（3/3）。

---

## 七、多选账号与导出账号（新增）

### 双击登录 / 右键菜单

- **双击某个账号 = 直接登录它**（等于选中它再点「自动登录所选账号」，会先关掉当前 Steam 再切过去）；
  说明：双击**最左边「选择」列**不会登录（那一列双击是勾选），双击账号名/其它列都算登录。
- **右键某个账号 = 弹出菜单**：「登录该账号（双击也行）」「编辑该账号」「删除该账号」；
  右键时如果那一行不在当前多选里，会先把它选中；它已经在多选里则保留多选。
- 之前双击是"编辑"，现在编辑改到右键菜单和「编辑所选」按钮上。

### 多选账号

- 列表最左边新增 **「选择」列**：单击格子 = 勾选/取消（`■` / `□`）；**点该列表头 = 全选 / 全不选**；
- 表格右上角还有 **「全选」「清空勾选」** 按钮（只作用于已经加载出来的行，避免一次性勾上几千条）；
- 勾选之后，所有批量操作**自动以勾选为准**：「开始查询（查询所选）」「删除所选」「导出账号」；
  一个都没勾选时，仍然按 Ctrl / Shift 在列表里多选的那几行；
- 标题栏计数会显示「共 N 个账号，已勾选 M 个」。

### 仅显示无风险

表格右上角现在有两个筛选，**互斥**（勾一个会自动取消另一个）：**「仅显示有风险/异常结果」**、**「仅显示无风险」**。

进入筛选模式时会清空已有勾选和行选中；关闭筛选仅刷新列表，保留当前勾选。这既避免删除、查询所选与导出误作用于隐藏记录，也不会在恢复全部列表时再次丢掉选择。

「无风险」= **查询成功且没有任何封禁**（VAC / 游戏封禁 / 社区封禁 / 库存封禁都为 0）；「未查询」的账号不算无风险。
标题栏会显示当前筛选，例如「共 20 个账号（仅无风险）」。

### 导出账号

点「导出账号」会**先弹窗让你选格式**（CSV / JSON / TXT），再让你选保存位置，文件名后缀自动跟格式一致：

| 格式 | 内容 |
|---|---|
| CSV | `account,password,steam_id,note` —— 可以直接再导入回来 |
| TXT | `账号----密码----SteamID64`（还没有解析出 ID 时就是 `账号----密码`） |
| JSON | `[{"account":…,"password":…,"steam_id":…,"note":…}]` |

- 导出范围：**勾选/选中的账号优先**；没有选中但当前正处于筛选状态时，格式对话框会默认勾上「仅导出当前筛选结果」，也可以手动取消后导出全部；未筛选且未选中时导出全部；
- 导出前会提醒：文件里是**明文密码**，只放自己机器上；
- 自测里加了往返用例：导出 TXT → 再导入 → 账号/密码/SteamID64 完全一致。

---

## 八、技术说明（给后续维护用）

原 exe 没有源码（PyInstaller 打包的 Python 3.14.0 程序）。本次是从字节码**反推源码**后再修改的，并用「重新编译 + 逐条字节码比对」的方式验证：145 个代码对象中，除本次有意修改的部分外，其余与原程序**逐条指令一致**（数据库层、DPAPI 加密、封禁查询、CSV 解析、自测等）。

新增/改动的文件：
- `steam_ban_manager.py`：主程序（约 2165 行），改动集中在 CSV 列识别、AccountStore（新增 `password_enc` 字段 / DPAPI 加解密 / `account_password()`）、账号对话框（新增密码输入框）、`open_steam_login`（改成自动登录）、导入进度提示文案。
- `steam_login.py`：新增模块，负责 Steam 客户端控制（定位安装目录、读写 `loginusers.vdf`、结束 Steam、带账号密码启动）。

自测：`Steam封禁批量查询器-自动登录版.exe --self-test` 返回 0（覆盖导入 1200 行、密码加密与解密回读、明文不入库、封禁结果写入、分页、API Key 加解密）。
